

Additional routes to mount (before express.json for Stripe ones):
  const verifiedProWebhook = require("./routes/verified_pro_webhook");
  app.use("/webhook/verified-pro", verifiedProWebhook);

Admin & Analytics routes (after express.json):
  const adminInvoices = require("./routes/admin_invoices"); app.use("/admin/invoices", auth, adminInvoices);
  const adminAnalytics = require("./routes/admin_analytics"); app.use("/admin/analytics", auth, adminAnalytics);

Cron:
  require("./cron/reminders"); // loads daily reminders
